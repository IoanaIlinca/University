from valid import *
from repo import *
from model import *
                  
def srv_add_expense(ap, number, typ, amount):
    #function that adds an expense based on apartment number, type of expense and the amount
    #input: ap - list of apartments, number, amount - integers, typ - string
    #output: -

    add_expense(ap, number, typ, amount) # change - use validation here



